package model.characters.creatures;

import model.characters.Character;

public class Creature extends Character {
    public Creature(String _name, int _hp, int _power, int _initiative) {
        super(_name, _hp, _power, _initiative);
    }
}