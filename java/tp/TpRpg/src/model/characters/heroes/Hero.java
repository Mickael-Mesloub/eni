package model.characters.heroes;

import model.characters.Character;

public class Hero extends Character {
    public Hero(String _name, int _hp, int _power, int _initiative) {
        super(_name, _hp, _power, _initiative);
    }
}
