spring:
  application:
    name: "cave-a-vin-mongodb"
  # Data source
  data:
    mongodb:
      username: sa
      password: MotDePasse
      host: localhost
      port: 27017
      database: TP_CAVE_A_VIN_MONGODB
      authentication-database: admin

# Log
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG